@model InkInc.Models.Photo

@{
    ViewData["Title"] = "Delete";
}

<h3>Are you sure you want to delete this photo?</h3>

<div>
    <hr />
    @*<dl class="row">
            <dt class = "col-sm-2">
                File name
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.FilePath)
            </dd>
        </dl>*@

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Yes, I'm sure" class="btn btn-danger" /> |
        <a asp-action="Details" asp-controller="Users" asp-route-id="@Model.UserId">Back to Profile</a>
    </form>
</div>
